POSTGRES_DB=domus_finance
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

API_PORT=8080
DB_PORT=5432
FRONTEND_ORIGIN=http://localhost:5173

# Chave de assinatura do token — mínimo 32 caracteres.
# Valor abaixo destina-se apenas a desenvolvimento local.
JWT_CHAVE=chave-de-desenvolvimento-substituir-em-producao-32c
JWT_EMISSOR=DomusFinance
JWT_AUDIENCIA=DomusFinanceApp
JWT_HORAS_VALIDADE=8
